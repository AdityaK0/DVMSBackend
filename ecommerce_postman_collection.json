{
  "info": {
    "name": "E-Commerce API",
    "_postman_id": "cb32f9d1-9141-4d2a-9e16-faa02f62d9d4",
    "description": "Postman collection for testing E-Commerce project APIs (Users, Vendors, Products, Orders, Payments).",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "Users (Auth)",
      "item": [
        {
          "name": "Register",
          "request": { "method": "POST", "url": "{{base_url}}/auth/register/" }
        },
        {
          "name": "Login",
          "request": { "method": "POST", "url": "{{base_url}}/auth/login/" }
        },
        {
          "name": "Token Refresh",
          "request": { "method": "POST", "url": "{{base_url}}/auth/refresh/" }
        },
        {
          "name": "Profile",
          "request": { "method": "GET", "url": "{{base_url}}/auth/profile/" }
        },
        {
          "name": "Addresses List/Create",
          "request": { "method": "GET", "url": "{{base_url}}/auth/addresses/" }
        },
        {
          "name": "Address Detail",
          "request": { "method": "GET", "url": "{{base_url}}/auth/addresses/1/" }
        }
      ]
    },
    {
      "name": "Vendors",
      "item": [
        {
          "name": "Vendor List",
          "request": { "method": "GET", "url": "{{base_url}}/vendors/" }
        },
        {
          "name": "Vendor Create",
          "request": { "method": "POST", "url": "{{base_url}}/vendors/create/" }
        },
        {
          "name": "Vendor Profile",
          "request": { "method": "GET", "url": "{{base_url}}/vendors/profile/" }
        },
        {
          "name": "Vendor Detail",
          "request": { "method": "GET", "url": "{{base_url}}/vendors/1/" }
        },
        {
          "name": "Vendor Catalog",
          "request": { "method": "GET", "url": "{{base_url}}/vendors/1/catalog/" }
        }
      ]
    },
    {
      "name": "Products",
      "item": [
        {
          "name": "Product List",
          "request": { "method": "GET", "url": "{{base_url}}/products/" }
        },
        {
          "name": "Product Create",
          "request": { "method": "POST", "url": "{{base_url}}/products/create/" }
        },
        {
          "name": "Product Detail",
          "request": { "method": "GET", "url": "{{base_url}}/products/1/" }
        },
        {
          "name": "Product Update",
          "request": { "method": "PUT", "url": "{{base_url}}/products/1/update/" }
        },
        {
          "name": "Product Delete",
          "request": { "method": "DELETE", "url": "{{base_url}}/products/1/delete/" }
        },
        {
          "name": "Category List",
          "request": { "method": "GET", "url": "{{base_url}}/products/categories/" }
        }
      ]
    },
    {
      "name": "Orders",
      "item": [
        {
          "name": "Order List",
          "request": { "method": "GET", "url": "{{base_url}}/orders/" }
        },
        {
          "name": "Order Create",
          "request": { "method": "POST", "url": "{{base_url}}/orders/create/" }
        },
        {
          "name": "Order Detail",
          "request": { "method": "GET", "url": "{{base_url}}/orders/1/" }
        },
        {
          "name": "Order Cancel",
          "request": { "method": "POST", "url": "{{base_url}}/orders/1/cancel/" }
        }
      ]
    },
    {
      "name": "Payments",
      "item": [
        {
          "name": "Payment List",
          "request": { "method": "GET", "url": "{{base_url}}/payments/" }
        },
        {
          "name": "Payment Create",
          "request": { "method": "POST", "url": "{{base_url}}/payments/create/" }
        },
        {
          "name": "Payment Detail",
          "request": { "method": "GET", "url": "{{base_url}}/payments/1/" }
        }
      ]
    }
  ],
  "variable": [
    {
      "key": "base_url",
      "value": "http://127.0.0.1:8000/api"
    }
  ]
}
